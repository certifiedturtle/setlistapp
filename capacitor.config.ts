import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.certifiedturtle.setlistbuddy',
  appName: 'Setlist Buddy',
  webDir: 'dist'
};

export default config;
